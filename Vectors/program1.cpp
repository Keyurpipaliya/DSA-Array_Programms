// Write an program of insert Elements numbers in c++

#include <iostream>
#include <vector>

using namespace std;

int main() {
    int arr[10] = {10, 20, 30, 40, 50};
    int n;

    // inititalize is Elements of numbers
    vector<int> vector = {10, 20, 30, 40, 50};
    
    v.push_back(5);

    <begin int> = [150];

    cout<<"Enter is insert Elements numbers"<<":"<<endl;

    return 0;
}

