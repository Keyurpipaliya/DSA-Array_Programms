// Write an program of sortsqude program into array of integers and sort ascending and desceding order it.

#include <iostream>

using namespace std;

int main() {

    // it create is array of Numbers Elements
    int arr[] = {20, 10, 7, 5, 2};
    int n = 6; 
    int i;

    for (int i = 0; i < n; i++)
    {
       cout<<"Enter is Sort Square "<<":"<<endl;
       cin>>sortsquare;
    }
    
    for (int j = i; j < n - i; j++)
    {
        if (arr[i] != arr[i + 1])
        {
            cout<<"Enter is ascending order of numbers"<<endl;
        }
        else {
            cout<<"Enter is Descending order of numbers"<<endl;
        }
    }
    
    return 0;
}