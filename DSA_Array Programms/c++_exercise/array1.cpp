// write an number is all sum of all evennumbers.
#include <iostream>

using namespace std;

int main() {

    // C++ programm array an sum is All Even numbers.
    // Declare an array with even numbers.


    int arr[] = {2, 4, 6, 8, 10};
    int number;
    int i;

    cout<<"Even numbers between 2 and 10"<<numbers<<endl;
    cin>>arr;

    cout<<"Enter is \n numbers between 2 and"<<numbers<<endl;
    for(number=1; number<=5; number++) 
    { 
        for(number%2==0) {
            cout<<number<<"";
            sum = sum + number;
        }
    }

    cout<<"The sum of all Even Numbers"<<numbers<<sum;

    return 0;
}